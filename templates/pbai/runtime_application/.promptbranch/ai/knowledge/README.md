# Authoritative domain context

This directory contains the reviewed knowledge and project context for `example-runtime`. Replace this template text with authoritative, versioned domain sources before production use.
