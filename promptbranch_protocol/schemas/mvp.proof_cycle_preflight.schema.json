{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://promptbranch.local/schemas/mvp.proof_cycle_preflight.schema.json",
  "title": "Promptbranch MVP proof-cycle preflight",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "schema_version",
    "cycle",
    "release_mode",
    "version",
    "baseline_version",
    "next_version",
    "repo_id",
    "artifact",
    "artifact_sha256",
    "checks",
    "details",
    "failed_checks",
    "ok",
    "status",
    "preflight_sha256"
  ],
  "properties": {
    "schema": {"const": "promptbranch.mvp.proof_cycle_preflight"},
    "schema_version": {"const": "1.1"},
    "cycle": {"type": "integer", "minimum": 1},
    "release_mode": {"const": "normal"},
    "version": {"type": "string", "pattern": "^v[0-9]+\\.[0-9]+\\.[0-9]+$"},
    "baseline_version": {"type": "string", "pattern": "^v[0-9]+\\.[0-9]+\\.[0-9]+(?:\\.[0-9]+)*$"},
    "next_version": {"type": "string", "pattern": "^v[0-9]+\\.[0-9]+\\.[0-9]+$"},
    "repo_id": {"type": "string", "minLength": 1},
    "artifact": {"type": "string", "minLength": 1},
    "artifact_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "checks": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {"type": "boolean"}
    },
    "details": {"type": "object"},
    "failed_checks": {
      "type": "array",
      "items": {"type": "string"},
      "uniqueItems": true
    },
    "ok": {"type": "boolean"},
    "status": {"enum": ["mvp_proof_preflight_passed", "mvp_proof_preflight_failed"]},
    "preflight_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
  }
}
