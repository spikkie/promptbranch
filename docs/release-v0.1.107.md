# Release v0.1.107

Defines the controlled correction execution envelope as deterministic design data only.

The release adds `pb loop execution-envelope-design`, the machine-readable project design record, exact target/file/operation/hash/validation/rollback/limit/timeout/evidence fields, and a canonical design fingerprint.

No correction is executed. No workspace is created. No file, repository, deployment, Kubernetes resource, Project Source, artifact registry, or ChatGPT Project is mutated by the loop command.

Fresh `full_direct`, independent `full_localhost`, sandbox 13/13, all external-live gates, Artifact Guardian, and evidence-bound adoption remain mandatory.
