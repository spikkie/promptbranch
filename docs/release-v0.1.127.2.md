# v0.1.127.2 — Consolidated v0.1.127 closure and single-Python lifecycle repair

Baseline: `v0.1.126.1.1.1.1.3` (accepted/current).

Purpose: close the complete v0.1.127 normal slice through one consolidated repair candidate rather than continue the deep dotted repair chain.

## Included v0.1.127 scope

- portable deterministic `promptbranch-tool-authoring` skill/export with proposal-only authority;
- canonical ChatGPT project identity and immutable artifact conversation provenance;
- exact executed baseline ask-route proof at `TESTED_GREEN` and independent verification;
- defined acceptance-path project-conversation predicate with correlated provenance preservation;
- exact launcher-Python authority across runtime preparation, validation, release contracts, pipeline, and release-set execution.

## Python identity correction

`Path.resolve()` is forbidden for selecting the release interpreter. A pipx/venv launcher may be a symlink to `/usr/bin/python3.12`; executing the target is not equivalent to executing the venv launcher. `v0.1.127.2` executes the exact launcher path and removes alternate Python authority selectors.

## Closure

Construction must satisfy DOD-510 through DOD-514. The v0.1.127 family closes only when DOD-515 is live-proven through `FINAL_VERIFIED` and fresh scoped current alignment.

Out of scope: v0.1.128 environment-hardening work, external application mutation, response-completion redesign, and general legacy cleanup unrelated to v0.1.127 closure.

## Construction result

All 17 canonical deterministic release-validation groups passed on the exact extracted candidate; deterministic rebuilds are byte-identical and Artifact Guardian reports `release_ready=true`. Live DOD-515 remains the only closure item.
