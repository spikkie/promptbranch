# v0.1.103.10.44 — repair release-live challenge fail-fast logging and stop live cascade

Scope: repair release-live challenge fail-fast logging and stop live cascades after `ask_live` reports `docker_live_profile_challenged`. Preserves all-in-Docker, explicit live profile bootstrap, live pool preservation, `/c/...` conversation URLs, `--retries 0`, and no host-CDP/session-manager.
