# v0.1.123.2.5 — Two-component ZIP output plus reply-envelope contract

Repair-only release from accepted/current `v0.1.123.2.4`. The generated `ask-release` prompt now begins with the manually proven final-response contract: Component 1 is one rendered downloadable ZIP outside the envelope; Component 2 is exactly one marked Promptbranch JSON envelope. The request schema declares the same ordered two-component contract. JSON-only sandbox declarations are explicitly invalid and failure remains fail-closed with `artifacts=[]`.

No timeout, adoption, Project Source, Git, or proof-count semantics advance.
