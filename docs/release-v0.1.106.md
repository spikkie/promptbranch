# Release v0.1.106

`v0.1.106 — Controlled correction promotion decision record`

This normal slice adds `pb loop promotion-decision`. The command reruns the accepted three-run sandbox readiness assessment and records exactly `go` or `no_go` from 32 mandatory evidence and safety checks.

The included project decision record is GO because all three sandbox runs are complete, use distinct temporary workspaces, share one canonical fingerprint, pass every existing sandbox gate, and retain zero broader authority.

GO authorizes only `v0.1.107 — Controlled correction execution envelope design`. It does not grant correction execution, disposable- or real-repository mutation, deployment, Kubernetes, Project Source, artifact-adoption, or Project-deletion authority.

Machine-readable record:

`docs/project/correction-promotion-decision-v0.1.106.json`
