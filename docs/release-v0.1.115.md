# Promptbranch v0.1.115

## Scope

`v0.1.115 — PBAI-001 operational validation, lifecycle evidence, and impact-based fast testing`

This candidate starts from accepted/current `v0.1.114.2` and adds:

- a tamper-evident `promptbranch.ai.operational_lifecycle_evidence` contract;
- post-adoption PBAI operational proof covering correction, strict lifecycle, Project Source publication, evidence-bound adoption, accepted/current verification, rollback and recovery;
- automatic operational-evidence generation and validation after successful strict adoption;
- `pb test impacted` with a checked-in file-to-test-group map, dependency closure, fail-closed unmapped files, JSON selection evidence and exact-key evidence reuse bound to source content, test definitions, Git revisions, runtime/dependency identity and platform identity;
- edit, component and candidate development modes while preserving the mandatory strict 10/10 release/adoption gate.

## Authority

- Accepted/current before this candidate: `v0.1.114.2`.
- Candidate: `chatgpt_claudecode_workflow-2_v0.1.115.zip`.
- Strict host validation and adoption remain pending.
- No fast-development result may claim release acceptance.
