# Release candidate v0.1.99.1

`v0.1.99.1` is a repair-only candidate for the failed `v0.1.99` rolling slice horizon release. It repairs Docker build-context freshness and fail-fast diagnostics without advancing scope.

The active normal slice remains `v0.1.99 — Rolling slice horizon and architecture-decision protocol`; `v0.1.100 — First controlled read-only validation command execution` remains deferred until this repair line is accepted/current.
